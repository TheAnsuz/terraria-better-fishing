namespace BetterFishing.Config.Model
{
    public enum AnglerShopMode
    {
        DISABLED, // Deactivated
        SIMPLE, // No requirement
        PROGRESSIVE, // Quest requiremenet
        CONDITIONAL, // Only conditions applied
        COMPLEX, // Conditions and quest requirement
    }
}
